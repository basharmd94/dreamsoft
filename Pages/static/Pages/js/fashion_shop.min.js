(function () {
    'use strict';

    // ✅ Guard: Wait for GSAP + ScrollTrigger to be fully loaded
    function waitForGsap(callback) {
        if (typeof window.gsap !== 'undefined' && typeof window.ScrollTrigger !== 'undefined') {
            callback();
        } else {
            // Fallback: retry after short delay (handles edge cases with defer timing)
            setTimeout(() => waitForGsap(callback), 50);
        }
    }

    function initHeroAnimations() {
        if (typeof window.gsap === 'undefined' || typeof window.ScrollTrigger === 'undefined') {
            return;
        }

        if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
            return;
        }

        window.gsap.from('.fs-hero .hero-content', {
            scrollTrigger: { trigger: '.fs-hero', start: 'top 85%', once: true },
            opacity: 0, x: -50, duration: 0.8, ease: 'power3.out',
        });

        window.gsap.from('.fs-hero .hero-image-wrapper', {
            scrollTrigger: { trigger: '.fs-hero', start: 'top 85%', once: true },
            opacity: 0, x: 50, duration: 0.8, delay: 0.2, ease: 'power3.out',
        });
    }

    function initOverviewAnimations() {
        if (typeof window.gsap === 'undefined' || typeof window.ScrollTrigger === 'undefined') {
            return;
        }

        if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
            return;
        }

        window.gsap.from('.fs-overview-section .fs-image-col', {
            scrollTrigger: { trigger: '.fs-overview-section', start: 'top 75%', once: true },
            opacity: 0, x: -50, duration: 0.8, ease: 'power3.out',
        });

        window.gsap.from('.fs-overview-section .fs-content-col', {
            scrollTrigger: { trigger: '.fs-overview-section', start: 'top 75%', once: true },
            opacity: 0, x: 50, duration: 0.8, delay: 0.15, ease: 'power3.out',
        });
    }

    function initFeaturesAnimations() {
        if (typeof window.gsap === 'undefined' || typeof window.ScrollTrigger === 'undefined') {
            return;
        }

        if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
            return;
        }

        window.gsap.from('.fs-features-section .fs-section-header', {
            scrollTrigger: { trigger: '.fs-features-section', start: 'top 75%', once: true },
            opacity: 0, y: 30, duration: 0.7, ease: 'power3.out',
        });

        window.gsap.from('.fs-features-section .fs-card', {
            scrollTrigger: { trigger: '.fs-grid-6', start: 'top 80%', once: true },
            opacity: 0, y: 30, duration: 0.5, stagger: 0.08, ease: 'power3.out',
        });
    }

    function initInventoryAnimations() {
        if (typeof window.gsap === 'undefined' || typeof window.ScrollTrigger === 'undefined') {
            return;
        }

        if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
            return;
        }

        window.gsap.from('.fs-inventory-section .fs-content-col', {
            scrollTrigger: { trigger: '.fs-inventory-section', start: 'top 75%', once: true },
            opacity: 0, x: -50, duration: 0.8, ease: 'power3.out',
        });

        window.gsap.from('.fs-inventory-section .fs-image-col', {
            scrollTrigger: { trigger: '.fs-inventory-section', start: 'top 75%', once: true },
            opacity: 0, x: 50, duration: 0.8, delay: 0.15, ease: 'power3.out',
        });

        window.gsap.from('.fs-inventory-section .fs-feature-box', {
            scrollTrigger: { trigger: '.fs-inventory-section .fs-content-col', start: 'top 60%', once: true },
            opacity: 0, x: -30, duration: 0.5, stagger: 0.1, delay: 0.3, ease: 'power3.out',
        });
    }

    function initBillingAnimations() {
        if (typeof window.gsap === 'undefined' || typeof window.ScrollTrigger === 'undefined') {
            return;
        }

        if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
            return;
        }

        window.gsap.from('.fs-billing-section .fs-section-header', {
            scrollTrigger: { trigger: '.fs-billing-section', start: 'top 75%', once: true },
            opacity: 0, y: 30, duration: 0.7, ease: 'power3.out',
        });

        window.gsap.from('.fs-billing-section .fs-image-col', {
            scrollTrigger: { trigger: '.fs-billing-section .fs-two-col-grid', start: 'top 75%', once: true },
            opacity: 0, x: -50, duration: 0.8, ease: 'power3.out',
        });

        window.gsap.from('.fs-billing-section .fs-content-col', {
            scrollTrigger: { trigger: '.fs-billing-section .fs-two-col-grid', start: 'top 75%', once: true },
            opacity: 0, x: 50, duration: 0.8, delay: 0.15, ease: 'power3.out',
        });

        window.gsap.from('.fs-billing-section .fs-check-list li', {
            scrollTrigger: { trigger: '.fs-billing-section .fs-check-list', start: 'top 85%', once: true },
            opacity: 0, x: -20, duration: 0.4, stagger: 0.06, delay: 0.3, ease: 'power3.out',
        });
    }

    function initAnalyticsAnimations() {
        if (typeof window.gsap === 'undefined' || typeof window.ScrollTrigger === 'undefined') {
            return;
        }

        if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
            return;
        }

        window.gsap.from('.fs-analytics-section .fs-image-col', {
            scrollTrigger: { trigger: '.fs-analytics-section', start: 'top 75%', once: true },
            opacity: 0, x: -50, duration: 0.8, ease: 'power3.out',
        });

        window.gsap.from('.fs-analytics-section .fs-content-col', {
            scrollTrigger: { trigger: '.fs-analytics-section', start: 'top 75%', once: true },
            opacity: 0, x: 50, duration: 0.8, delay: 0.15, ease: 'power3.out',
        });
    }

    function initErpAnimations() {
        if (typeof window.gsap === 'undefined' || typeof window.ScrollTrigger === 'undefined') {
            return;
        }

        if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
            return;
        }

        window.gsap.from('.fs-erp-section .fs-content-col', {
            scrollTrigger: { trigger: '.fs-erp-section', start: 'top 75%', once: true },
            opacity: 0, x: -50, duration: 0.8, ease: 'power3.out',
        });

        window.gsap.from('.fs-erp-section .fs-image-col', {
            scrollTrigger: { trigger: '.fs-erp-section', start: 'top 75%', once: true },
            opacity: 0, x: 50, duration: 0.8, delay: 0.15, ease: 'power3.out',
        });

        window.gsap.from('.fs-erp-section .fs-check-list li', {
            scrollTrigger: { trigger: '.fs-erp-section .fs-check-list', start: 'top 85%', once: true },
            opacity: 0, y: 20, duration: 0.4, stagger: 0.06, delay: 0.2, ease: 'power3.out',
        });
    }

    function initBoutiqueAnimations() {
        if (typeof window.gsap === 'undefined' || typeof window.ScrollTrigger === 'undefined') {
            return;
        }

        if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
            return;
        }

        window.gsap.from('.fs-boutique-section .fs-content-col', {
            scrollTrigger: { trigger: '.fs-boutique-section', start: 'top 75%', once: true },
            opacity: 0, x: -50, duration: 0.8, ease: 'power3.out',
        });

        window.gsap.from('.fs-boutique-section .fs-image-col', {
            scrollTrigger: { trigger: '.fs-boutique-section', start: 'top 75%', once: true },
            opacity: 0, x: 50, duration: 0.8, delay: 0.15, ease: 'power3.out',
        });
    }

    function initLocalAnimations() {
        if (typeof window.gsap === 'undefined' || typeof window.ScrollTrigger === 'undefined') {
            return;
        }

        if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
            return;
        }

        window.gsap.from('.fs-local-section .fs-image-col', {
            scrollTrigger: { trigger: '.fs-local-section', start: 'top 75%', once: true },
            opacity: 0, x: -50, duration: 0.8, ease: 'power3.out',
        });

        window.gsap.from('.fs-local-section .fs-content-col', {
            scrollTrigger: { trigger: '.fs-local-section', start: 'top 75%', once: true },
            opacity: 0, x: 50, duration: 0.8, delay: 0.15, ease: 'power3.out',
        });
    }

    function initFaqAnimations() {
        // FAQ accordion logic runs regardless of motion preference
        const faqItems = document.querySelectorAll('.fs-faq-item');

        faqItems.forEach((item) => {
            const question = item.querySelector('.fs-faq-question');
            if (!question) return;

            question.addEventListener('click', () => {
                const isOpen = item.classList.contains('is-open');

                // Close all other items
                faqItems.forEach((other) => {
                    if (other !== item) {
                        other.classList.remove('is-open');
                        const otherQuestion = other.querySelector('.fs-faq-question');
                        if (otherQuestion) {
                            otherQuestion.setAttribute('aria-expanded', 'false');
                        }
                    }
                });

                // Toggle current item
                item.classList.toggle('is-open');
                question.setAttribute('aria-expanded', !isOpen);
            });
        });

        // GSAP reveal animation (respects reduced motion)
        if (typeof window.gsap === 'undefined' || typeof window.ScrollTrigger === 'undefined') {
            return;
        }

        if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
            return;
        }

        window.gsap.from('.fs-faq-header', {
            scrollTrigger: { trigger: '.fs-faq-section', start: 'top 75%', once: true },
            opacity: 0, y: 30, duration: 0.7, ease: 'power3.out',
        });

        window.gsap.from('.fs-faq-item', {
            scrollTrigger: { trigger: '.fs-faq-list', start: 'top 80%', once: true },
            opacity: 0, y: 20, duration: 0.5, stagger: 0.08, ease: 'power3.out',
        });
    }

    function initThoughtsAnimations() {
        if (typeof window.gsap === 'undefined' || typeof window.ScrollTrigger === 'undefined') {
            return;
        }

        if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
            return;
        }

        window.gsap.from('.fs-thoughts-content', {
            scrollTrigger: { trigger: '.fs-thoughts-section', start: 'top 75%', once: true },
            opacity: 0, x: -50, duration: 0.8, ease: 'power3.out',
        });

        window.gsap.from('.fs-thoughts-visual', {
            scrollTrigger: { trigger: '.fs-thoughts-section', start: 'top 75%', once: true },
            opacity: 0, x: 50, duration: 0.8, delay: 0.15, ease: 'power3.out',
        });
    }

    function initCtaAnimations() {
        if (typeof window.gsap === 'undefined' || typeof window.ScrollTrigger === 'undefined') {
            return;
        }

        if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
            return;
        }

        window.gsap.from('.fs-final-cta', {
            scrollTrigger: { trigger: '.fs-cta-section', start: 'top 80%', once: true },
            opacity: 0, y: 40, duration: 0.8, ease: 'power3.out',
        });
    }

    // ✅ Main init: Run after DOM is ready AND GSAP is loaded
    function initFashionShop() {
        // Register ScrollTrigger plugin once (idempotent, but centralized for clarity)
        if (typeof window.gsap !== 'undefined' && typeof window.ScrollTrigger !== 'undefined') {
            window.gsap.registerPlugin(window.ScrollTrigger);
        }

        initHeroAnimations();
        initOverviewAnimations();
        initFeaturesAnimations();
        initInventoryAnimations();
        initBillingAnimations();
        initAnalyticsAnimations();
        initErpAnimations();
        initBoutiqueAnimations();
        initLocalAnimations();
        initFaqAnimations();
        initThoughtsAnimations();
        initCtaAnimations();

        // Refresh ScrollTrigger after page load (images/fonts ready)
        window.addEventListener('load', () => {
            if (window.ScrollTrigger) {
                window.ScrollTrigger.refresh();
            }
        });
    }

    // Primary: DOMContentLoaded (fires after deferred scripts execute)
    document.addEventListener('DOMContentLoaded', function () {
        waitForGsap(initFashionShop);
    });

    // Fallback: If DOMContentLoaded already fired (edge case), run immediately
    if (document.readyState === 'interactive' || document.readyState === 'complete') {
        waitForGsap(initFashionShop);
    }
})();